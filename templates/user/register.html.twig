{% extends 'base.html.twig' %}

{% block title %} S'inscrire {% endblock %}

{% block body %}

<section id="register" class="main-section">
        <h1 class="headingband resp-hsize">S'inscrire sur le weblog de Gabriel G. pour pouvoir poster des commentaires</h1>
        {{ form_start(registerform) }}
        {{ form_row(registerform.login, {'attr' : {'placeholder' : "Login " }}) }}
        {{ form_row(registerform.password, {'attr' : {'placeholder' : "Mot de passe " }}) }}
        {{ form_row(registerform.confirm_password, {'attr' : {'placeholder' : "Confirmez votre password " }}) }}
        {{ form_row(registerform.email, {'attr' : {'placeholder' : "Adresse e-mail " }}) }}
        {{ form_row(registerform.confirm_email, {'attr' : {'placeholder' : "Confirmez votre e-mail " }}) }}
        {{ form_row(registerform.newsletter_subscriber, {'attr' : {'placeholder' : "Souscrire à la newsletter ? "  }}) }}
        <button type="submit" class="bouton">
            S'inscrire
        </button>
        {{ form_end(registerform) }}
    </section>

{% endblock %}